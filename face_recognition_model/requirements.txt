torch==2.0.0  # Ensure compatibility with CUDA 12.4
torchvision==0.15.0
opencv-python==4.8.0.74
sqlite3  # SQLite is usually included with Python, so no need to specify this unless using an external package
